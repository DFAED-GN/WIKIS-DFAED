services:
  mediawiki:
    build: .
    restart: always
    ports:
      - 8080:80
    links:
      - database
    volumes:
      - /var/www/html/images
      - ./LocalSettings.php:/var/www/html/LocalSettings.php
      - ./staging_area:/var/www/html/staging_area
      - ./imports_externes:/var/www/html/imports_externes
    environment:
      - MW_ADMIN_USER=admin
      - MW_ADMIN_PASS=admin123
      - MW_DB_SERVER=database
      - MW_DB_NAME=my_wiki
      - MW_DB_USER=wikiuser
      - MW_DB_PASS=wikipass
      - MW_SITE_NAME=Documentation FAED v3
      # Pour l'installation initiale, on peut avoir besoin d'accéder au script d'install
      # Une fois configuré, le LocalSettings.php devra être monté ici.

  database:
    image: mariadb:10.4
    restart: always
    environment:
      - MYSQL_DATABASE=my_wiki
      - MYSQL_USER=wikiuser
      - MYSQL_PASSWORD=wikipass
      - MYSQL_RANDOM_ROOT_PASSWORD=yes
    volumes:
      - db_data_123:/var/lib/mysql

volumes:
  db_data_123:
